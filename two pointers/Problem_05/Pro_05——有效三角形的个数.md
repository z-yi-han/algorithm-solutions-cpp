#### 读题
用两边之和＞第三边来判断是否为有效三角形
#### 算法思路
首先先知道如何判断三角形是有效的,其实只要两个小的加起来＞大的即可判断,因此先对非负数组进行排序即可  
然后其实就转化成了三个数字,下面只需要确定一个值,然后判断他前面的数组能否构成有效三角形,并用ret记录,,如果是那么right不动这个区间全部满足,因此直接right-- 
然后在计算过程中,这组数字如果＜=记录,那么左指针++,相反,则记录